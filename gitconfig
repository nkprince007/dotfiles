# This is Git's per-user configuration file.
[user]
        name = Naveen Kumar Sangi
        email = naveenkumarsangi@protonmail.com
        signingkey = D853830889E423BA
[core]
        editor = vim
        whitespace = fix,-indent-with-non-tab,trailing-space,cr-at-eol
	excludesfile = /Users/naveenkumarsangi/.gitignore_global
[push]
        default = matching
[color]
        ui = auto
[color "branch"]
        current = yellow bold
        local = green bold
        remote = cyan bold
[color "diff"]
        meta = yellow bold
        frag = magenta bold
        old = red bold
        new = green bold
        whitespace = red reverse
[color "status"]
        added = green bold
        changed = yellow bold
        untracked = red bold
[diff]
        tool = vimdiff
[difftool]  
        prompt = false
[alias] a = add --all
        b = branch
        c = commit -S
        commit = commit -S
        d = diff
        f = fetch 
        fo = fetch origin
        pom = pull origin master
        l = log --oneline
        lg = log --oneline --graph --decorate --branches --tags --remotes
        co = checkout
        r = reset
        hr = reset --hard HEAD
        rb = rebase
        rba = rebase --abort
        rbc = rebase --continue
        rbm = rebase --interactive master
        s = status
        cge = config --global --edit
        ss = stash save
        sa = stash apply
        sc = stash clear
        rpo = remote prune origin
        rao = remote add origin
        contrib = shortlog --summary --numbered
        undo = reset HEAD~1 --mixed
        save = git add -A && git commit -v -m 'SAVEPOINT'
        fc = "!f() { git log --pretty=format:'%C(yellow)%h  %Cblue%ad  %Creset%s%Cgreen  [%cn] %Cred%d' --decorate --date=short -S$1; }; f"
        fm = "!f() { git log --pretty=format:'%C(yellow)%h  %Cblue%ad  %Creset%s%Cgreen  [%cn] %Cred%d' --decorate --date=short --grep=$1; }; f"
[merge]
	tool = code
[commit]
	gpgsign = true
[gpg]
	program = gpg
[advice]
	statusHints = false
